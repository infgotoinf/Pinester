<Window x:Class="Pinester.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Pinester"
        mc:Ignorable="d"
        Title="Pinester" Height="600" Width="1040">

    <Window.Resources>
        <Style x:Key="NormButtonStyle" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="Black"/>
            <Setter Property="FontFamily" Value="Fira Mono"/>
            <Setter Property="Opacity" Value="0.25"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="DarkSlateGray" 
                                BorderThickness="0" 
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Opacity" Value="0.75"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <Style x:Key="NormScrollViewerStyle" TargetType="ScrollViewer">
            <Setter Property="Background" Value="#404040"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollViewer">
                        <Border Background="{TemplateBinding Background}"
                                BorderThickness="0">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="*"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <ScrollContentPresenter Grid.Row="0" Grid.Column="0"/>

                                <ScrollBar x:Name="PART_VerticalScrollBar"
                                           Grid.Row="0" Grid.Column="1"
                                           Value="{TemplateBinding VerticalOffset}"
                                           Maximum="{TemplateBinding ScrollableHeight}"
                                           ViewportSize="{TemplateBinding ViewportHeight}"
                                           Visibility="{TemplateBinding ComputedVerticalScrollBarVisibility}"/>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ScrollBar">
            <Setter Property="Background" Value="Black"/>
            <Setter Property="Opacity" Value="0.4"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Border Background="Black">
                            <Grid>
                                <Track x:Name="PART_Track">
                                    <Track.Thumb>
                                        <Thumb Background="White" Opacity="0.5" BorderThickness="0"/>
                                    </Track.Thumb>
                                </Track>
                            </Grid>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="#FF313131">
        <ScrollViewer Style="{StaticResource NormScrollViewerStyle}"
                      Margin="10,60,10,10">
            <!-- Отступ сверху для кнопок -->
            <ItemsControl x:Name="ImageContainer">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <local:MasonryPanel ColumnWidth="185"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <!-- Шаблон отображает ImageSource из объекта ImageInfo -->
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="5" Width="180" CornerRadius="10" ClipToBounds="True">
                            <Image Source="{Binding ImageSource}" Stretch="UniformToFill" />
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Панель с кнопками для добавления файлов и папок -->
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="20,10,0,0">
            <Button Content="Добавить файлы" Style="{StaticResource NormButtonStyle}"
                    Command="{Binding AddFilesCommand}" 
                    Height="36" Width="150"/>

            <Button Content="Добавить папку" Style="{StaticResource NormButtonStyle}"
                    Command="{Binding AddFolderCommand}"
                    Height="36" Width="150"/>
        </StackPanel>

        <Rectangle Stroke="#FF313131" StrokeThickness="50" RadiusX="50" RadiusY="50" Margin="-40,-40,-178,-40"/>
    </Grid>
</Window>
    
    